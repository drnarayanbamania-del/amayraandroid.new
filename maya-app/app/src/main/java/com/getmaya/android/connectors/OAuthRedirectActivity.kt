package com.getmaya.android.connectors

import android.app.Activity

/** Handles OAuth redirects (maya://oauth) from GitHub etc. */
class OAuthRedirectActivity : Activity() {
    // TODO: exchange code from intent.data, store token in encrypted prefs
}
